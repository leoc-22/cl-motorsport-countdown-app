{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.2.3",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply countdown-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply countdown-db --remote"
  },
  "dependencies": {
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "wrangler": "^4.46.0"
  }
}
